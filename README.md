# flick-site

Landing page for [Flick](https://github.com/nerdydude364/flick), a minimal media viewer.

## Deploy

Run `./deploy.sh` to:

- fetch the latest release from GitHub
- update the version badge in `public/index.html`
- download release assets to `public/release/`

Serve the `public/` folder as static files.
